# Partition_Count_Generator

./partition_generator.exe -n {some number} //paste this into terminal to generate values f(1) to f(some number) 


./partition_generator.exe -v {n1} {n2} {n3} ... //paste this to generate specific values f(n1), f(n2), f(n3) ...
